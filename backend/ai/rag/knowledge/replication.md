# Replication

Replication factor is the desired number of copies of an object. Replica placement and actual replica health are runtime state and must be read from Vault metadata or integrity tools; documentation cannot establish how many healthy replicas currently exist.