# Failure Recovery

Node health and replica availability can change during partial failure. Inspect live node status and object metadata before explaining a degradation. Replica repair and node recovery modify system state and require an explicit backend-recorded approval.