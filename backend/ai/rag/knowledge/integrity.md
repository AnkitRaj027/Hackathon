# Integrity

Integrity verification compares the expected object hash with hashes reported for its replicas. Only the current verify_integrity result can identify which replicas are valid or corrupted; never infer corruption from generic guidance.